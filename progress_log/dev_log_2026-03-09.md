# 开发日志 - 2026-03-09

## 概述

本次会话主要解决了Flutter构建超时和SQLite原生库缺失问题，成功构建并安装APK到真机。

---

## 完成事项

### 1. Flutter国内镜像配置

**问题**：Gradle从Google服务器下载Flutter原生库超时（1小时41分钟后失败）

**解决方案**：配置Flutter中国镜像环境变量

```powershell
# 永久设置（用户级别）
[System.Environment]::SetEnvironmentVariable('PUB_HOSTED_URL', 'https://pub.flutter-io.cn', 'User')
[System.Environment]::SetEnvironmentVariable('FLUTTER_STORAGE_BASE_URL', 'https://storage.flutter-io.cn', 'User')
```

**效果**：
- 构建时间从 1h41min(失败) → 2.7min(成功)
- 下载源从 Google → 中国镜像

---

### 2. SQLite原生库修复

**问题**：
```
Failed to load dynamic library 'libsqlite3.so': not found
```

**原因**：`database_manager.dart` 使用 `NativeDatabase.createInBackground()` 但未正确初始化 `sqlite3_flutter_libs` 的原生库。

**修复文件**：`lib/data/database/database_manager.dart`

```dart
// 添加导入
import 'package:sqlite3/sqlite3.dart';
import 'package:sqlite3_flutter_libs/sqlite3_flutter_libs.dart';

// 在创建数据库前初始化原生库
static AppDatabase _createDatabase() {
  final executor = LazyDatabase(() async {
    // 在Android上，确保sqlite3_flutter_libs的原生库被正确加载
    if (Platform.isAndroid) {
      sqlite3.openInMemory().dispose();
    }

    final dbFolder = await getApplicationDocumentsDirectory();
    final file = File(p.join(dbFolder.path, 'body_controller.db'));
    return NativeDatabase.createInBackground(file);
  });
  return AppDatabase(executor);
}
```

---

### 3. APK构建与安装

**构建命令**：
```powershell
flutter build apk --debug
```

**构建结果**：
- APK路径：`build/app/outputs/flutter-apk/app-debug.apk`
- APK大小：143 MB
- 构建时间：约2.7分钟

**安装命令**：
```powershell
adb install -r build/app/outputs/flutter-apk/app-debug.apk
```

**目标设备**：
- 设备ID：23127PN0CC
- 厂商：Xiaomi
- Android版本：Android 16 (API 36)
- 架构：android-arm64

---

## 问题诊断过程

### 问题1：Gradle下载超时

1. 检查Java进程网络连接 → 发现连接到Google服务器
2. 确认镜像配置 → Gradle和Maven镜像已配置，但Flutter环境变量未设置
3. 解决方案 → 设置 `FLUTTER_STORAGE_BASE_URL` 环境变量

### 问题2：SQLite库缺失

1. 查看截图错误信息 → `libsqlite3.so not found`
2. 检查 `pubspec.yaml` → `sqlite3_flutter_libs` 已在依赖中
3. 检查 `database_manager.dart` → 发现未调用初始化
4. 解决方案 → 添加 `sqlite3.openInMemory().dispose()` 初始化

---

## 更好的调试方式

使用 `flutter run` 实时调试，避免每次手动安装APK：

```powershell
flutter run -d 83bd71ec
```

优势：
- 实时查看日志输出
- 支持热重载（修改代码后立即生效）
- 无需每次重新安装

---

## 待办事项

- [ ] 测试训练记录页面功能
- [ ] 测试力量训练页面功能
- [ ] 测试AI教练功能
- [ ] 测试目标追踪功能
- [ ] 构建Release版本APK

---

## 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `lib/data/database/database_manager.dart` | 修改 | 添加SQLite原生库初始化 |
| `progress_log/implementation_progress.md` | 修改 | 追加本次会话总结 |
| `progress_log/dev_log_2026-03-09.md` | 新建 | 本文件 |

---

## 技术要点

### Flutter镜像环境变量

| 变量 | 值 | 用途 |
|------|-----|------|
| `PUB_HOSTED_URL` | `https://pub.flutter-io.cn` | Dart包镜像 |
| `FLUTTER_STORAGE_BASE_URL` | `https://storage.flutter-io.cn` | Flutter原生库镜像 |

### Drift数据库初始化

Drift在Android上使用需要：
1. 添加 `sqlite3_flutter_libs` 依赖
2. 在使用前初始化原生库（`sqlite3.openInMemory().dispose()`）
3. 使用 `NativeDatabase.createInBackground()` 创建数据库

---

### 4. 最终修复方案：drift_sqflite

**问题**：上述SQLite初始化方案仍失败，`sqlite3_flutter_libs 0.6.0+eol` 是废弃包，不包含原生库。

**根本原因**：
- `sqlite3_flutter_libs` 0.6.0+eol 是空包，仅作为兼容性占位
- `sqlite3` 2.x版本不包含Flutter原生库
- APK中缺少 `libsqlite3.so`

**最终解决方案**：使用 `drift_sqflite` + `sqflite`

**修改文件**：

1. `pubspec.yaml`：
```yaml
# 移除
# sqlite3_flutter_libs: ^0.6.0+eol

# 添加
drift_sqflite: ^2.0.0
```

2. `lib/data/database/database_manager.dart`：
```dart
import 'package:drift/drift.dart';
import 'package:drift_sqflite/drift_sqflite.dart';
import 'package:path_provider/path_provider.dart';
import 'package:path/path.dart' as p;
import 'database.dart';

// ...

static AppDatabase _createDatabase() {
  final executor = LazyDatabase(() async {
    final dbFolder = await getApplicationDocumentsDirectory();
    final file = p.join(dbFolder.path, 'body_controller.db');
    return SqfliteQueryExecutor(path: file);
  });
  return AppDatabase(executor);
}
```

**原理**：
- `sqflite` 使用Android系统内置的SQLite（`/system/lib/libsqlite.so`）
- 无需打包额外的原生库，避免兼容性问题

**结果**：训练记录页面正常加载，无错误

---

## 最终文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `pubspec.yaml` | 修改 | 替换 sqlite3_flutter_libs 为 drift_sqflite |
| `lib/data/database/database_manager.dart` | 修改 | 使用 SqfliteQueryExecutor |
| `progress_log/implementation_progress.md` | 修改 | 追加本次会话总结 |
| `progress_log/dev_log_2026-03-09.md` | 新建 | 本文件 |

---

## 经验总结

### SQLite方案对比

| 方案 | 优点 | 缺点 | 推荐 |
|------|------|------|------|
| `sqlite3_flutter_libs` | 独立SQLite版本 | 0.6.0+eol已废弃 | ❌ |
| `sqlite3` 3.x + Flutter | 新版内置支持 | 依赖冲突多 | ⚠️ |
| `drift_sqflite` + sqflite | 使用系统SQLite，稳定 | 依赖系统版本 | ✅ |

### 教训
1. 遇到原生库问题时，优先考虑使用系统内置库
2. 检查依赖包的维护状态（eol标记）
3. 使用成熟的解决方案（sqflite是Flutter最成熟的SQLite方案）

---

**创建时间**：2026-03-09
**最后更新**：2026-03-09 23:30
**会话时长**：约1小时
